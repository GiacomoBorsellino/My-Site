setTimeout(() => {
  document.body.getElementsByClassName("cardContainer")[0].style.opacity = 1;
  document.body
    .getElementsByClassName("cardContainer")[0]
    .classList.add("rotateCard");
}, 0);

setTimeout(() => {
  document.body.getElementsByClassName("cardContainer")[1].style.opacity = 1;
  document.body
    .getElementsByClassName("cardContainer")[1]
    .classList.add("rotateCard");
}, 200);

setTimeout(() => {
  document.body.getElementsByClassName("cardContainer")[2].style.opacity = 1;
  document.body
    .getElementsByClassName("cardContainer")[2]
    .classList.add("rotateCard");
}, 400);

setTimeout(() => {
  document.body.getElementsByClassName("cardContainer")[3].style.opacity = 1;
  document.body
    .getElementsByClassName("cardContainer")[3]
    .classList.add("rotateCard");
}, 600);

setTimeout(() => {
  document.body.getElementsByClassName("cardContainer")[4].style.opacity = 1;
  document.body
    .getElementsByClassName("cardContainer")[4]
    .classList.add("rotateCard");
}, 800);

setTimeout(() => {
  document.body.getElementsByClassName("cardContainer")[5].style.opacity = 1;
  document.body
    .getElementsByClassName("cardContainer")[5]
    .classList.add("rotateCard");
}, 1000);

setTimeout(() => {
  document.body.getElementsByClassName("cardContainer")[6].style.opacity = 1;
  document.body
    .getElementsByClassName("cardContainer")[6]
    .classList.add("rotateCard");
}, 1200);

setTimeout(() => {
  document.body.getElementsByClassName("cardContainer")[7].style.opacity = 1;
  document.body
    .getElementsByClassName("cardContainer")[7]
    .classList.add("rotateCard");
}, 1400);

setTimeout(() => {
  document.body.getElementsByClassName("cardContainer")[8].style.opacity = 1;
  document.body
    .getElementsByClassName("cardContainer")[8]
    .classList.add("rotateCard");
}, 1400);
